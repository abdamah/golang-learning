# Lesson 06 — Exercises

**Author:** Abdallah

## Run the example

```bash
go run .
```

## Exercises

1. Switch on `status := 404` — print `OK` for 200, `Not found` for 404, `Error` for anything else.
2. Switch on `role := "admin"` — print a message for `admin`, `user`, and default `guest`.
3. Switch on `method := "POST"` — print `Creating a note` for POST.

## Check your solution

```bash
go run solution06.go
```
